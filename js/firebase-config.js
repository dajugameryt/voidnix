// Firebase stub file to avoid missing-script/MIME errors in production.
// Facebook login is currently disabled in index.js, so these stay null by default.
window.auth = null;
window.facebookProvider = null;
window.firebaseConfigured = false;
